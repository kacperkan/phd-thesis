\begin{figure}
  \centering
  \includegraphics[width=\linewidth]{fig/\conerfdirname/assets/ablations/laplacian.pdf}
  \caption{{\bf Using Laplacian loss $\loss{lap}$ -- }
    \kk{
      Applying the additional regularizer helps the network to produce consistent masking when interpolating between attributes.
      At the same time, it prevents appearance of spurious artifacts in the
      color space.
    }
  }
  \label{fig:conerf-ablation_laplacian}
\end{figure}